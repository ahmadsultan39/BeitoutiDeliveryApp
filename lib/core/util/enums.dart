enum Gender {
  male,
  female,
}

enum AccessibilityStaysType {
  approved,
  notApproved,
  notRegistered,
  notVerified,
  blocked,
  isRejected,
}

enum TransportationType {
  bicycle,
  electricBicycle,
  motoCycle,
  car,
}

enum OrderStatus {
  pending,
  approved,
  notApproved,
  prepared,
  failedAssigning,
  picked,
  delivered,
  notDelivered,
  canceled,
}
